spring.application.name=chimera
# Worker is enabled when Redis is enabled by default.
# This allows tests to override worker behavior without requiring a real Redis instance.
chimera.worker.enabled=${chimera.redis.enabled:true}
