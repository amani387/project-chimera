spring.application.name=chimera
# Worker and Judge are enabled when Redis is enabled by default.
# This allows tests to override behavior without requiring a real Redis instance.
chimera.worker.enabled=${chimera.redis.enabled:true}
chimera.judge.enabled=${chimera.redis.enabled:true}
